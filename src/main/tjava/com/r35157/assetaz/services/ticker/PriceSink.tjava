package com.r35157.assetaz.services.ticker;

import com.r35157.assetaz.services.ticker.plugins.ps.PriceSource;
import org.jetbrains.annotations.NotNull;

import java.math.BigDecimal;
import java.time.Instant;

/**
 * Receives raw typed observations from price sources.
 */
@FunctionalInterface
public interface PriceSink {
    void announce(
            @NotNull PriceSource source,
            @NotNull ΩPriceΩ price,
            @NotNull Instant observedAt
    );
}
