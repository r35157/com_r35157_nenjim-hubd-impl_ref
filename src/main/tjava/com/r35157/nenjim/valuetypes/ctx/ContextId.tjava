package com.r35157.nenjim.valuetypes.ctx;

//import com.r35157.libs.valuetypes.basic.Id;
import org.jetbrains.annotations.NotNull;

public record ContextId(
    @NotNull String value
) { //implements Id {
    public static ContextId of(String contextId) {
        return new ContextId(contextId);
    }
}
