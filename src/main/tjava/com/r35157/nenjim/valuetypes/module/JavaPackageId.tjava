package com.r35157.nenjim.valuetypes.module;

//import com.r35157.libs.valuetypes.basic.Id;
import org.jetbrains.annotations.NotNull;

public record JavaPackageId(
    @NotNull String value
) { //implements Id {
    public static JavaPackageId of(String javaPackageId) {
        return new JavaPackageId(javaPackageId);
    }
}
